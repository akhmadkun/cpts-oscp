# 1

Match log file type with its appropriate use case.

log file : 

splunkd.log
metrics.log
CiscoSecurityCloud.log


use case:
    - Helps to troubleshoot connectivity issues, authentication errors, API errors, and data ingestion problems.

    - Helps to troubleshoot and identify API connection issues, data parsing errors, and app-specific issues.

    - Determine the cause of ingestion bottlenecks, monitor the system performance, and resolve indexing delays or throttling issues.

# 2

According to the Connectivity panel under the Data Integrity dashboard, the Cisco Secure Endpoint is connected but sends zero megabytes of data to the Cisco Security Cloud app. What could be the two causes of the problem? (Choose two.)

- The endpoint sends data, but the Security Cloud app has a display bug showing zero megabytes.
    
- The Security Cloud app requires a manual refresh every 30 minutes to receive data.
    
- Splunk might not be recognizing the logs because they are empty or malformed.
    
- The endpoint is in sleep mode, and the Secure Endpoint does not send data when a device is inactive.
    
- It might be that the data is being dropped due to a parsing error.

# 3
What will happen if you try to install a new version using the install from file option without clicking the Upgrade App option, if an older version of the Cisco Security Cloud app is already installed?

- It will pop a message stating the need to choose the **Upgrade App** option.
    
- It will prompt you to agree to first de-install the current version.
    
- It will overwrite the old version and install the new version.
    
- It will install the new version and also keep the current version.

